import { MeetmountainValidators } from './meetmountain-validators';

describe('MeetmountainValidators', () => {
  it('should create an instance', () => {
    expect(new MeetmountainValidators()).toBeTruthy();
  });
});
